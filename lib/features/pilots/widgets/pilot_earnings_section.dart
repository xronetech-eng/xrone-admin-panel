import 'package:flutter/widgets.dart';

class PilotEarningsSection extends StatelessWidget {
  const PilotEarningsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

import 'package:flutter/widgets.dart';

class PilotKycSection extends StatelessWidget {
  const PilotKycSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

import 'package:flutter/widgets.dart';

class PilotServicesSection extends StatelessWidget {
  const PilotServicesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

import 'package:flutter/widgets.dart';

class PilotDroneSection extends StatelessWidget {
  const PilotDroneSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

import 'package:flutter/widgets.dart';

class PilotBankSection extends StatelessWidget {
  const PilotBankSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

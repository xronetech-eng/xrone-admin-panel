import 'package:flutter/widgets.dart';

class UserOrdersSection extends StatelessWidget {
  const UserOrdersSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

import 'package:flutter/widgets.dart';

class UserTransactionsSection extends StatelessWidget {
  const UserTransactionsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
